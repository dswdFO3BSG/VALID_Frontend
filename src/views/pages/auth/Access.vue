<template>
    <div class="bg-surface-50 dark:bg-surface-950 flex items-center justify-center min-h-screen min-w-[100vw] overflow-hidden">
        <div class="flex flex-col items-center justify-center">
            <div style="border-radius: 56px; padding: 0.3rem; background: linear-gradient(180deg, rgba(239, 68, 68, 0.4) 10%, rgba(239, 68, 68, 0) 30%)">
                <div class="w-full bg-surface-0 dark:bg-surface-900 py-20 px-8 sm:px-20 flex flex-col items-center" style="border-radius: 53px">
                    <div class="gap-4 flex flex-col items-center">
                        <div class="flex justify-center items-center border-2 border-red-500 rounded-full" style="width: 3.2rem; height: 3.2rem">
                            <i class="text-red-500 pi pi-fw pi-lock !text-2xl"></i>
                        </div>
                        <h1 class="text-surface-900 dark:text-surface-0 font-bold text-4xl lg:text-5xl mb-2">Access Denied</h1>
                        <span class="text-muted-color mb-8">You do not have the necessary permisions. Please contact admins.</span>
                        <div class="col-span-12 mt-8 text-center">
                            <Button @click="logout" label="Logout" severity="danger" />
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</template>

<script setup>
import AuthServices from '@/service/authentication/AuthServices';
import Cookies from 'js-cookie';
import { Button } from 'primevue';

const logout = async () => {
    await AuthServices.logout({});
    Cookies.remove('psa-token');
    Cookies.remove('token_valid');
    Cookies.remove('empNo');
    Cookies.remove('name');
    window.location.replace('/');
};
</script>
